package com.woolab.lumella.contracttests

import kotlin.test.Test
import kotlin.test.assertTrue

/**
 * Sanity check that the contract-tests module wires up and runs (P2 scaffold).
 * Real compatibility tests against the luma engine land in P3.
 */
class ScaffoldSanityTest {
    @Test
    fun scaffoldRuns() {
        assertTrue(true)
    }
}
